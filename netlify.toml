# Netlify serves the Mini App (index.html) as a static site. No build step is needed.
[build]
  publish = "."
  command = ""

# Always fetch the latest Mini App so updates show up right away in Telegram
[[headers]]
  for = "/index.html"
  [headers.values]
    Cache-Control = "no-cache"
